# Database URL; defaults to sqlite:///./recipes.db if not set
DB_URL=sqlite:///./recipes.db

# Frontend origin for CORS (optional); defaults to http://localhost:3000
FRONTEND_ORIGIN=http://localhost:3000

# Seed demo data on start if database is empty (true/false)
SEED_ON_START=true
